[options]

cifar10 ood = lsunc lsunr svhn const32 uniform32 average

ood_methods = baseline odin*


[DEFAULT]

type = vib
dataset = cifar10

wim_sets = not

# done = 500..


activation = relu

features = conv32 vgg19

batch_norm = both encoder

warmup = 50
data_augmentation = flip crop

test_latent_sampling = 128

latent_sampling = 1


latent_dim = 256 1024

[filters]
