[filters]

type = vae 
dataset = cifar10

prior_distribution = gaussian
# features = conv32
depth = 1

output_distribution = gaussian

# warmup = 100

sigma_train = learned
data_augmentation = flip crop

test_latent_sampling = 128

[sets]

# cifar10 = mnist32r fashion32r lsunr svhn const32
cifar10 = lsunr svhn  average const32